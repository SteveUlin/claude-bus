use flake
# Repo root for scripts, layouts, and the spawn helper. Shell scripts
# derive this themselves from $0 location; exporting it here means
# `zellij --layout ...` invocations from any cwd still find the bus.
export CLAUDE_BUS_ROOT="$PWD"
